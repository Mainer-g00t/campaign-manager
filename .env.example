PORT=3001
# URL this app is reachable at — used as the CDR callback_url sent to the VoIP platform
APP_URL=http://campaign-manager:3001
DATABASE_URL=file:./data/campaign-manager.db
# Base URL of the voice AI config-api (reachable via shared Docker network)
VOIP_BASE_URL=http://config-api:8080
